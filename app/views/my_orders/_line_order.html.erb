<%= link_to my_order_path(order), class: "order" do %>
  <div class="d-flex mb-4 mt-4">
    <div class="col-6 ps-3 mb-3 d-flex flex-column justify-content-between">
      <h5 class="mt-3"><%= order.created_at.to_date %></h5>
      <p>Order Total: $<%= order.total_cents / 100.0 %></p>
    </div>
  </div>
<% end%>